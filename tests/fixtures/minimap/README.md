# Mini-map fixtures

Task 4 uses generated arrays so segmentation expectations stay exact and independent of
one game frame. Add captured mini-map crops here only as regression evidence, with their
resolution and capture conditions documented; production HSV and morphology settings
remain configurable in `config/default.yaml`.
